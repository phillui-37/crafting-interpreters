class Math {
    square(n) {
        return n*n;
    }
}

class Math2 < Math {
    cook(n) {
        var test = super.square(n) / 2;
        return test;
    }
}

fun sq2(n) {return n*n;}

var m = Math2();
print "This is " + m.toString();
print m.square(3) == sq2(3);
print m.cook(3);

fun fib(n) {
  if (n < 2) return n;
  return fib(n - 1) + fib(n - 2);
}

var before = clock();
print fib(40);
var after = clock();
print after - before;